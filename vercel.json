{
    "version": 2,
    "buildCommand": "cd apps/corporate-hub && npm install && npm run build",
    "installCommand": "cd apps/corporate-hub && npm install",
    "framework": "nextjs",
    "outputDirectory": "apps/corporate-hub/.next",
    "name": "ledger-snap-v1",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/$1"
        }
    ]
}