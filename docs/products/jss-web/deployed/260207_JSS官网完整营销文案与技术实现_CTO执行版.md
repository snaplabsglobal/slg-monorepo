# JSS 官网完整营销文案与技术实现（CTO执行版）

> **文档类型：** 营销文案 + 前端代码  
> **关联文档：** 260207_JSS离线功能与SmartTrace完整技术规格_CTO执行版.md  
> **创建时间：** 2026-02-07  
> **优先级：** 🟡 P1 - 营销页面  
> **执行人：** CTO + 前端 + 市场团队

---

## 📋 执行摘要

**目标：**
- 3秒让装修老板看懂我们跟CompanyCam不是一类东西
- 建立"工地相机"而非"智能系统"的产品定位
- 完整的中英文双语支持

**交付物：**
- ✅ 5个screen完整文案（英文+中文）
- ✅ 可直接运行的React/Next.js代码
- ✅ 语言切换完整方案
- ✅ 移动端适配方案

**核心定位：**
> 我们站在工地这一边，不站在云端那一边

---

## 🎯 产品定位与核心叙事

### 核心立场

**一句话定位：**
```
The jobsite camera that works
when everything else doesn't.
```

**核心对比：**
```
Most apps assume you have internet.
JSS assumes you don't.
```

---

### 五屏叙事结构（完整闭环）

```
Hero首屏
  ↓
地下室场景（主战场）
  ↓
失败案例（别人为什么会坑你）
  ↓
Smart Trace（我们怎么帮忙但不越界）
  ↓
Why Switch（真实的人为什么信任我们）
```

---

## 📝 完整营销文案（中英文双语）

### 🟦 Hero首屏

#### H1 主标题

**English:**
```
The jobsite camera that works
when everything else doesn't.
```

**中文:**
```
在所有东西都不靠谱的时候，
它依然能用的工地相机。
```

---

#### H2 副标题

**English:**
```
Built for basements, bad signal, and real job sites —
not offices with Wi-Fi.
```

**中文:**
```
为地下室、没信号、真实工地而生，
不是为有 Wi-Fi 的办公室。
```

---

#### 一句话价值锚

**English:**
```
No signal? Still counts.
Shot taken = evidence saved.
```

**中文:**
```
没信号？照样算数。
按下快门 = 证据已保存。
```

---

#### 核心立场

**English:**
```
Most apps assume you have internet.
JSS assumes you don't.
```

**中文:**
```
大多数 App 假设你有网络，
JSS 假设你没有。
```

---

#### 三个核心点

**1. Offline-first by design / 离线优先设计**

**English:**
```
Photos are saved locally first. 
Upload never blocks the shot.
```

**中文:**
```
先写本地，上传永远不能卡拍照。
```

---

**2. Camera-grade reliability / 相机级可靠性**

**English:**
```
If it's written to the device, it's real. Period.
```

**中文:**
```
只要写进设备，就是真的。
```

---

**3. No silent automation / 没有偷偷自动化**

**English:**
```
We don't move, rename, or reassign 
your photos without you.
```

**中文:**
```
没你点头，系统什么都不改。
```

---

#### CTA

**English:**
```
Try it where Wi-Fi dies →
```

**中文:**
```
在没信号的地方试试 →
```

---

### 🟦 第二屏：真实地下室场景

#### H2 标题

**English:**
```
Basement. No signal. Still counts.
```

**中文:**
```
地下室。没信号。照样算数。
```

---

#### 场景文案

**English:**
```
You're in a basement.
Concrete walls. No bars. No Wi-Fi.

You take photos because you have to —
progress, changes, proof.

With most apps, this is where things get risky.
With JSS, this is where it just works.
```

**中文:**
```
你在地下室。
水泥墙，零信号，没有 Wi-Fi。

你拍照不是因为想拍，
而是因为必须拍：进度、变更、证据。

对大多数 App 来说，这一刻开始变得危险。
对 JSS 来说，这一刻才刚刚正常。
```

---

#### 对比一句

**English:**
```
Other apps: You hope it saved.
JSS: The shot already counts.
```

**中文:**
```
别的 App：你只能祈祷它存下来了。
JSS：快门一按，已经算数。
```

---

#### 钉子句

**English:**
```
If it's written to the device,
it's evidence — even underground.
```

**中文:**
```
只要写进设备，
它就是证据 —— 哪怕在地下室。
```

---

### 🟦 第三屏：3个真实失败案例

#### 主标题

**English:**
```
When photos fail, it's never obvious.
Until it's too late.
```

**中文:**
```
照片出问题时，
你往往是最后一个知道的。
```

---

#### 失败案例1：上传卡死

**English:**
```
Title: Upload stuck
Subtitle: You thought it synced. It didn't.

Reasons:
- Photos tied to upload
- Retry queues get stuck
- No clear failure signal

JSS Solution:
JSS saves first. Upload failure is a state — not a loss.
```

**中文:**
```
标题：上传卡死
副标题：你以为传了，其实没有。

原因：
- 拍照和上传绑在一起
- 重试队列卡死
- 没有明确失败提示

JSS 做法：
先保存，上传失败只是状态，不是否定。
```

---

#### 失败案例2：误归档

**English:**
```
Title: Wrong project
Subtitle: The system 'helped' — and guessed wrong.

Reasons:
- Silent auto-archiving
- No explicit confirmation
- One mistake breaks trust

JSS Solution:
JSS never auto-assigns in Phase 1. You always confirm.
```

**中文:**
```
标题：误归档
副标题：系统帮忙了，但猜错了。

原因：
- 后台自动归档
- 没有用户确认
- 一次猜错，信任全无

JSS 做法：
Phase 1 不自动归档，一切由你确认。
```

---

#### 失败案例3：丢证据

**English:**
```
Title: Lost evidence
Subtitle: Uninstall = gone forever.

Reasons:
- No system gallery
- Unclear upload state
- False sense of safety

JSS Solution:
JSS clearly shows what's saved and what's not.
```

**中文:**
```
标题：丢证据
副标题：卸载 = 永久消失。

原因：
- 不写系统相册
- 状态不透明
- 误以为已经安全

JSS 做法：
清楚告诉你哪些已保存，哪些还没。
```

---

#### 总结一句

**English:**
```
Failure doesn't happen when you take the photo.
It happens when you think you're done.
```

**中文:**
```
失败不是发生在拍照时，
而是发生在你以为已经没事的时候。
```

---

### 🟦 第四屏：Smart Trace

#### H2 标题

**English:**
```
Smart Trace remembers. You decide.
```

**中文:**
```
Smart Trace 记得清楚，决定权在你。
```

---

#### 核心说明

**English:**
```
You take photos offline.
Later, JSS connects the dots — carefully.

Not by guessing.
Not by changing anything behind your back.

Just a clear suggestion, waiting for you.
```

**中文:**
```
你离线拍照。
联网后，JSS 才开始对账。

不猜、不偷改、不自作主张。
只是给你一个清楚的建议，等你点头。
```

---

#### UI示例文案

**English:**
```
"Photos taken near: West 41st Ave Project (42m)"

[Confirm] [Not this job]

Nothing happens until you tap.
```

**中文:**
```
"发现照片拍摄位置接近：West 41st Ave 项目（42 米）"

[确认] [不是这个工地]

你不点，什么都不会发生。
```

---

#### 钉子句

**English:**
```
If you didn't decide it, it didn't change.
```

**中文:**
```
不是你决定的，系统就不会改。
```

---

### 🟦 第五屏：Why Contractors Switch

#### 三句真实反馈

**English:**
```
"I stopped worrying if the photo actually saved."
— Basement reno contractor, Vancouver

"It doesn't try to be smart behind my back."
— General contractor, Burnaby

"I trust it when things go wrong — 
not when everything's perfect."
— Project manager, Lower Mainland
```

**中文:**
```
"我不再担心照片到底有没有存下来。"
— 地下室装修承包商，温哥华

"它不会背着我自作聪明。"
— 总承包商，本拿比

"出问题的时候，它也一样靠谱。"
— 项目经理，低陆平原
```

---

#### 收束一句

**English:**
```
People don't switch tools because of features.
They switch because of trust.
```

**中文:**
```
人换工具，不是因为功能，
而是因为信任。
```

---

#### 最终CTA

**English:**
```
Try JSS where Wi-Fi dies →
```

**中文:**
```
在没信号的地方试试 JSS →
```

---

## 🔧 技术实现方案

### 一、文件结构

```
/app
  /(marketing)
    /page.tsx
/components
  /landing
    LandingPage.tsx
    Header.tsx
    Section.tsx
    LanguageSwitch.tsx
/content
  copy.en.ts
  copy.zh.ts
  copy.types.ts
```

---

### 二、类型定义

**文件：** `content/copy.types.ts`

```typescript
export type Lang = "en" | "zh";

export type Copy = {
  hero: {
    h1: string;
    h2: string;
    value: string;
    stance: string;
    bullets: Array<{ title: string; desc: string }>;
    cta: string;
  };
  basement: {
    h2: string;
    body: string[];
    compare: { other: string; jss: string };
    anchor: string;
  };
  failures: {
    title: string;
    cases: Array<{
      title: string;
      subtitle: string;
      reason: string[];
      jss: string;
    }>;
    summary: string;
  };
  smartTrace: {
    h2: string;
    body: string[];
    uiExample: string;
    note: string;
    anchor: string;
  };
  whySwitch: {
    quotes: string[];
    summary: string;
    cta: string;
  };
};
```

---

### 三、英文文案文件

**文件：** `content/copy.en.ts`

```typescript
import type { Copy } from "./copy.types";

export const copyEn: Copy = {
  hero: {
    h1: "The jobsite camera that works when everything else doesn't.",
    h2: "Built for basements, bad signal, and real job sites — not offices with Wi-Fi.",
    value: "No signal? Still counts.\nShot taken = evidence saved.",
    stance: "Most apps assume you have internet. JSS assumes you don't.",
    bullets: [
      {
        title: "Offline-first by design",
        desc: "Photos are saved locally first. Upload never blocks the shot."
      },
      {
        title: "Camera-grade reliability",
        desc: "If it's written to the device, it's real. Period."
      },
      {
        title: "No silent automation",
        desc: "We don't move, rename, or reassign your photos without you."
      }
    ],
    cta: "Try it where Wi-Fi dies →"
  },

  basement: {
    h2: "Basement. No signal. Still counts.",
    body: [
      "You're in a basement. Concrete walls. No bars. No Wi-Fi.",
      "You take photos because you have to — progress, changes, proof.",
      "With most apps, this is where things get risky.",
      "With JSS, this is where it just works."
    ],
    compare: {
      other: "Other apps: You hope it saved.",
      jss: "JSS: The shot already counts."
    },
    anchor: "If it's written to the device, it's evidence — even underground."
  },

  failures: {
    title: "When photos fail, it's never obvious. Until it's too late.",
    cases: [
      {
        title: "Upload stuck",
        subtitle: "You thought it synced. It didn't.",
        reason: [
          "Photos tied to upload",
          "Retry queues get stuck",
          "No clear failure signal"
        ],
        jss: "JSS saves first. Upload failure is a state — not a loss."
      },
      {
        title: "Wrong project",
        subtitle: "The system 'helped' — and guessed wrong.",
        reason: [
          "Silent auto-archiving",
          "No explicit confirmation",
          "One mistake breaks trust"
        ],
        jss: "JSS never auto-assigns in Phase 1. You always confirm."
      },
      {
        title: "Lost evidence",
        subtitle: "Uninstall = gone forever.",
        reason: [
          "No system gallery",
          "Unclear upload state",
          "False sense of safety"
        ],
        jss: "JSS clearly shows what's saved and what's not."
      }
    ],
    summary: "Failure doesn't happen when you take the photo. It happens when you think you're done."
  },

  smartTrace: {
    h2: "Smart Trace remembers. You decide.",
    body: [
      "You take photos offline.",
      "Later, JSS connects the dots — carefully.",
      "Not by guessing. Not by changing anything behind your back.",
      "Just a clear suggestion, waiting for you."
    ],
    uiExample: "Photos taken near: West 41st Ave Project (42m)",
    note: "Nothing happens until you tap.",
    anchor: "If you didn't decide it, it didn't change."
  },

  whySwitch: {
    quotes: [
      "I stopped worrying if the photo actually saved.",
      "It doesn't try to be smart behind my back.",
      "I trust it when things go wrong — not when everything's perfect."
    ],
    summary: "People don't switch tools because of features. They switch because of trust.",
    cta: "Try JSS where Wi-Fi dies →"
  }
};
```

---

### 四、中文文案文件

**文件：** `content/copy.zh.ts`

```typescript
import type { Copy } from "./copy.types";

export const copyZh: Copy = {
  hero: {
    h1: "在所有东西都不靠谱的时候，它依然能用的工地相机。",
    h2: "为地下室、没信号、真实工地而生，不是为有 Wi-Fi 的办公室。",
    value: "没信号？照样算数。\n按下快门 = 证据已保存。",
    stance: "大多数 App 假设你有网络，JSS 假设你没有。",
    bullets: [
      {
        title: "离线优先设计",
        desc: "先写本地，上传永远不能卡拍照。"
      },
      {
        title: "相机级可靠性",
        desc: "只要写进设备，就是真的。"
      },
      {
        title: "没有偷偷自动化",
        desc: "没你点头，系统不会移动或重分配照片。"
      }
    ],
    cta: "在没信号的地方试试 →"
  },

  basement: {
    h2: "地下室。没信号。照样算数。",
    body: [
      "你在地下室。水泥墙，零信号，没有 Wi-Fi。",
      "你拍照不是因为想拍，而是必须拍：进度、变更、证据。",
      "对大多数 App 来说，这一刻开始变得危险。",
      "对 JSS 来说，这一刻才刚刚正常。"
    ],
    compare: {
      other: "别的 App：你只能祈祷它存下来了。",
      jss: "JSS：快门一按，已经算数。"
    },
    anchor: "只要写进设备，它就是证据 —— 哪怕在地下室。"
  },

  failures: {
    title: "照片出问题时，你往往是最后一个知道的。",
    cases: [
      {
        title: "上传卡死",
        subtitle: "你以为传了，其实没有。",
        reason: [
          "拍照和上传绑在一起",
          "重试队列卡死",
          "没有明确失败提示"
        ],
        jss: "JSS 先保存，上传失败只是状态，不是否定。"
      },
      {
        title: "误归档",
        subtitle: "系统帮忙了，但猜错了。",
        reason: [
          "后台自动归档",
          "没有用户确认",
          "一次猜错，信任全无"
        ],
        jss: "JSS Phase 1 不自动归档，一切由你确认。"
      },
      {
        title: "丢证据",
        subtitle: "卸载 = 永久消失。",
        reason: [
          "不写系统相册",
          "状态不透明",
          "误以为已经安全"
        ],
        jss: "JSS 清楚告诉你哪些已保存，哪些还没。"
      }
    ],
    summary: "失败不是发生在拍照时，而是发生在你以为已经没事的时候。"
  },

  smartTrace: {
    h2: "Smart Trace 记得清楚，决定权在你。",
    body: [
      "你离线拍照。",
      "联网后，JSS 才开始对账。",
      "不猜、不偷改、不自作主张。",
      "只是给你一个清楚的建议，等你点头。"
    ],
    uiExample: "发现照片拍摄位置接近：West 41st Ave 项目（42 米）",
    note: "你不点，什么都不会发生。",
    anchor: "不是你决定的，系统就不会改。"
  },

  whySwitch: {
    quotes: [
      "我不再担心照片到底有没有存下来。",
      "它不会背着我自作聪明。",
      "出问题的时候，它也一样靠谱。"
    ],
    summary: "人换工具，不是因为功能，而是因为信任。",
    cta: "在没信号的地方试试 JSS →"
  }
};
```

---

### 五、语言切换组件

**文件：** `components/landing/LanguageSwitch.tsx`

```typescript
"use client";

import * as React from "react";
import type { Lang } from "@/content/copy.types";

const LS_KEY = "jss_lang";

export function getInitialLang(): Lang {
  if (typeof window === "undefined") return "en";
  const stored = window.localStorage.getItem(LS_KEY);
  if (stored === "en" || stored === "zh") return stored;
  return "en"; // default English (no auto-detect)
}

export function persistLang(lang: Lang) {
  try {
    window.localStorage.setItem(LS_KEY, lang);
  } catch {
    // ignore
  }
}

export function LanguageSwitch(props: {
  lang: Lang;
  onChange: (lang: Lang) => void;
  variant?: "desktop" | "mobile";
}) {
  const { lang, onChange, variant = "desktop" } = props;

  return (
    <div className="inline-flex items-center gap-2 rounded-full border px-3 py-1 text-sm">
      <button
        type="button"
        className={lang === "en" ? "font-semibold" : "opacity-70 hover:opacity-100"}
        onClick={() => onChange("en")}
        aria-pressed={lang === "en"}
      >
        EN
      </button>
      <span className="opacity-30">|</span>
      <button
        type="button"
        className={lang === "zh" ? "font-semibold" : "opacity-70 hover:opacity-100"}
        onClick={() => onChange("zh")}
        aria-pressed={lang === "zh"}
      >
        中文
      </button>
    </div>
  );
}
```

---

### 六、Header组件（桌面+移动端）

**文件：** `components/landing/Header.tsx`

```typescript
"use client";

import * as React from "react";
import type { Lang, Copy } from "@/content/copy.types";
import { LanguageSwitch } from "./LanguageSwitch";

export function Header(props: {
  lang: Lang;
  copy: Copy;
  onLangChange: (lang: Lang) => void;
}) {
  const { lang, copy, onLangChange } = props;
  const [open, setOpen] = React.useState(false);

  React.useEffect(() => {
    const onKey = (e: KeyboardEvent) => e.key === "Escape" && setOpen(false);
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, []);

  return (
    <header className="sticky top-0 z-50 border-b bg-white/80 backdrop-blur">
      <div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-3">
        {/* Logo */}
        <a href="#top" className="flex items-center gap-2">
          <div className="h-8 w-8 rounded-xl border" />
          <div className="leading-tight">
            <div className="text-sm font-semibold">JobSite Snap</div>
            <div className="text-xs opacity-70">SnapEvidence</div>
          </div>
        </a>

        {/* Desktop */}
        <div className="hidden items-center gap-3 md:flex">
          <nav className="mr-2 flex items-center gap-4 text-sm opacity-80">
            <a href="#basement" className="hover:opacity-100">Offline</a>
            <a href="#failures" className="hover:opacity-100">Reliability</a>
            <a href="#smart-trace" className="hover:opacity-100">Smart Trace</a>
          </nav>

          <LanguageSwitch lang={lang} onChange={onLangChange} variant="desktop" />

          <a
            href="#cta"
            className="rounded-full border px-4 py-2 text-sm font-semibold hover:opacity-90"
          >
            {copy.hero.cta}
          </a>
        </div>

        {/* Mobile */}
        <div className="md:hidden">
          <button
            type="button"
            className="rounded-lg border px-3 py-2 text-sm"
            onClick={() => setOpen((v) => !v)}
            aria-expanded={open}
          >
            Menu
          </button>
        </div>
      </div>

      {/* Mobile panel */}
      {open && (
        <div id="mobile-menu" className="border-t bg-white">
          <div className="mx-auto flex max-w-6xl flex-col gap-3 px-4 py-4">
            <nav className="flex flex-col gap-2 text-sm">
              <a onClick={() => setOpen(false)} href="#basement" className="py-1">
                Offline
              </a>
              <a onClick={() => setOpen(false)} href="#failures" className="py-1">
                Reliability
              </a>
              <a onClick={() => setOpen(false)} href="#smart-trace" className="py-1">
                Smart Trace
              </a>
            </nav>

            <div className="pt-2">
              <LanguageSwitch lang={lang} onChange={onLangChange} variant="mobile" />
            </div>

            <a
              onClick={() => setOpen(false)}
              href="#cta"
              className="mt-1 inline-flex items-center justify-center rounded-xl border px-4 py-3 text-sm font-semibold"
            >
              {copy.hero.cta}
            </a>
          </div>
        </div>
      )}
    </header>
  );
}
```

---

### 七、Section组件

**文件：** `components/landing/Section.tsx`

```typescript
import * as React from "react";

export function Section(props: {
  id: string;
  eyebrow?: string;
  title: string;
  children: React.ReactNode;
  className?: string;
}) {
  const { id, eyebrow, title, children, className = "" } = props;
  
  return (
    <section id={id} className={`scroll-mt-24 px-4 py-16 ${className}`}>
      <div className="mx-auto max-w-6xl">
        {eyebrow && (
          <div className="text-xs font-semibold uppercase tracking-wider opacity-70">
            {eyebrow}
          </div>
        )}
        <h2 className="mt-2 text-2xl font-semibold md:text-3xl">{title}</h2>
        <div className="mt-6">{children}</div>
      </div>
    </section>
  );
}
```

---

### 八、主Landing Page组件

**文件：** `components/landing/LandingPage.tsx`

```typescript
"use client";

import * as React from "react";
import type { Lang } from "@/content/copy.types";
import { copyEn } from "@/content/copy.en";
import { copyZh } from "@/content/copy.zh";
import { getInitialLang, persistLang } from "./LanguageSwitch";
import { Header } from "./Header";
import { Section } from "./Section";

function useSmoothScroll() {
  return React.useCallback((id: string) => {
    const el = document.getElementById(id);
    if (!el) return;
    el.scrollIntoView({ behavior: "smooth", block: "start" });
  }, []);
}

export default function LandingPage() {
  const [lang, setLang] = React.useState<Lang>("en");
  const scrollTo = useSmoothScroll();

  React.useEffect(() => {
    setLang(getInitialLang());
  }, []);

  const copy = lang === "en" ? copyEn : copyZh;

  const onLangChange = (l: Lang) => {
    setLang(l);
    persistLang(l);
  };

  return (
    <div id="top" className="min-h-screen bg-white text-black">
      <Header lang={lang} copy={copy} onLangChange={onLangChange} />

      <main>
        {/* HERO */}
        <section className="px-4">
          <div className="mx-auto grid min-h-[calc(100vh-56px)] max-w-6xl items-center py-10 md:py-16">
            <div className="max-w-3xl">
              <h1 className="text-4xl font-semibold leading-tight md:text-6xl">
                {copy.hero.h1}
              </h1>
              
              <p className="mt-5 text-lg opacity-80 md:text-xl">
                {copy.hero.h2}
              </p>

              <div className="mt-6 whitespace-pre-line rounded-2xl border p-4 text-sm md:text-base">
                {copy.hero.value}
              </div>

              <p className="mt-4 text-sm opacity-80 md:text-base">
                {copy.hero.stance}
              </p>

              <div className="mt-7 grid gap-3 md:grid-cols-3">
                {copy.hero.bullets.map((b) => (
                  <div key={b.title} className="rounded-2xl border p-4">
                    <div className="text-sm font-semibold">{b.title}</div>
                    <div className="mt-1 text-sm opacity-80">{b.desc}</div>
                  </div>
                ))}
              </div>

              <div className="mt-8 flex flex-wrap items-center gap-3" id="cta">
                <button
                  type="button"
                  className="rounded-full border px-5 py-3 text-sm font-semibold hover:opacity-90"
                  onClick={() => scrollTo("basement")}
                >
                  {copy.hero.cta}
                </button>

                <button
                  type="button"
                  className="rounded-full px-5 py-3 text-sm font-semibold opacity-70 hover:opacity-100"
                  onClick={() => scrollTo("basement")}
                >
                  ↓ Scroll
                </button>
              </div>
            </div>
          </div>
        </section>

        {/* BASEMENT */}
        <Section 
          id="basement" 
          title={copy.basement.h2} 
          eyebrow="Real jobsite scenario" 
          className="border-t"
        >
          <div className="grid gap-10 md:grid-cols-2">
            <div className="space-y-3">
              {copy.basement.body.map((p) => (
                <p key={p} className="text-sm opacity-85 md:text-base">
                  {p}
                </p>
              ))}

              <div className="mt-5 rounded-2xl border p-4">
                <div className="text-sm font-semibold">
                  {copy.basement.compare.other}
                </div>
                <div className="mt-1 text-sm opacity-80">
                  {copy.basement.compare.jss}
                </div>
              </div>

              <div className="mt-5 text-sm font-semibold">
                {copy.basement.anchor}
              </div>
            </div>

            <div className="rounded-3xl border p-6">
              <div className="text-sm font-semibold">Basement Scene</div>
              <div className="mt-2 text-sm opacity-75">
                Replace with real basement image/video
              </div>
              <div className="mt-6 h-48 rounded-2xl border" />
            </div>
          </div>
        </Section>

        {/* FAILURES */}
        <Section 
          id="failures" 
          title={copy.failures.title} 
          eyebrow="Common failure modes" 
          className="border-t"
        >
          <div className="grid gap-4 md:grid-cols-3">
            {copy.failures.cases.map((c) => (
              <div key={c.title} className="rounded-3xl border p-5">
                <div className="text-sm font-semibold">{c.title}</div>
                <div className="mt-1 text-sm opacity-75">{c.subtitle}</div>

                <ul className="mt-4 list-disc space-y-1 pl-5 text-sm opacity-80">
                  {c.reason.map((r) => (
                    <li key={r}>{r}</li>
                  ))}
                </ul>

                <div className="mt-4 rounded-2xl border p-4 text-sm">
                  <div className="font-semibold">JSS</div>
                  <div className="mt-1 opacity-80">{c.jss}</div>
                </div>
              </div>
            ))}
          </div>

          <div className="mt-10 rounded-3xl border p-6 text-sm font-semibold">
            {copy.failures.summary}
          </div>
        </Section>

        {/* SMART TRACE */}
        <Section 
          id="smart-trace" 
          title={copy.smartTrace.h2} 
          eyebrow="Helps — never decides" 
          className="border-t"
        >
          <div className="grid gap-10 md:grid-cols-2">
            <div className="space-y-3">
              {copy.smartTrace.body.map((p) => (
                <p key={p} className="text-sm opacity-85 md:text-base">
                  {p}
                </p>
              ))}

              <div className="mt-5 rounded-2xl border p-4">
                <div className="text-sm font-semibold">
                  {copy.smartTrace.uiExample}
                </div>
                <div className="mt-3 flex gap-2">
                  <button className="rounded-full border px-4 py-2 text-sm font-semibold hover:opacity-90">
                    Confirm
                  </button>
                  <button className="rounded-full border px-4 py-2 text-sm opacity-80 hover:opacity-100">
                    Not this job
                  </button>
                </div>
                <div className="mt-3 text-sm opacity-75">
                  {copy.smartTrace.note}
                </div>
              </div>

              <div className="mt-5 text-sm font-semibold">
                {copy.smartTrace.anchor}
              </div>
            </div>

            <div className="rounded-3xl border p-6">
              <div className="text-sm font-semibold">Smart Trace Preview</div>
              <div className="mt-2 text-sm opacity-75">
                Replace with UI mock screenshot
              </div>
              <div className="mt-6 h-48 rounded-2xl border" />
            </div>
          </div>
        </Section>

        {/* WHY SWITCH */}
        <Section 
          id="why-switch" 
          title="Why contractors switch" 
          eyebrow="Real words" 
          className="border-t"
        >
          <div className="grid gap-4 md:grid-cols-3">
            {copy.whySwitch.quotes.map((q) => (
              <div key={q} className="rounded-3xl border p-6 text-sm font-semibold">
                "{q}"
              </div>
            ))}
          </div>

          <div className="mt-10 text-sm opacity-85 md:text-base">
            {copy.whySwitch.summary}
          </div>

          <div className="mt-6">
            <a
              href="#top"
              className="inline-flex rounded-full border px-5 py-3 text-sm font-semibold hover:opacity-90"
            >
              {copy.whySwitch.cta}
            </a>
          </div>
        </Section>

        <footer className="border-t px-4 py-10">
          <div className="mx-auto max-w-6xl text-sm opacity-70">
            © {new Date().getFullYear()} JobSite Snap. All rights reserved.
          </div>
        </footer>
      </main>
    </div>
  );
}
```

---

### 九、Next.js Page入口

**文件：** `app/(marketing)/page.tsx`

```typescript
import LandingPage from "@/components/landing/LandingPage";

export default function Page() {
  return <LandingPage />;
}
```

---

## 📋 语言策略决策

### 一、当前策略（Phase 1/2）

**默认语言：** English

**原因：**
```
北美contractor心智：
"如果一个工具连英文都不是主语言，
我会下意识当成小众工具"

你现在这一整套叙事（地下室/证据/失效场景）
英文比中文更"冷静、更可信"
```

---

**中文：** 可切换，不自动跳

**规则：**
- ❌ 不根据IP自动切
- ❌ 不根据浏览器自动切
- ✅ 用户自己点

**理由：**
> 这是一个信任细节：你不"自作主张"，你把控制权交给用户

---

### 二、语言切换UI设计

**桌面端Header：**
```
[JSS LOGO]        EN ▼        Try it →
```

**点击"EN ▼"展开：**
```
English
中文
```

---

**关键规则：**
```
❌ 不用国旗
❌ 不刷新页面
❌ 不丢滚动位置

✅ 用户主动选择
✅ 记住用户选择（localStorage）
✅ 只是内容替换
```

---

### 三、其他语言策略

**Phase 1/2：** 只做English + 中文

**未来考虑条件（满足2条以上）：**
- ✅ 某个非英语用户群主动问你有没有他们的语言
- ✅ Sales/Onboarding中语言成为真实阻力
- ✅ 你已经在一个国家/地区持续有收入
- ✅ 有本地partner/reseller

**❌ 不构成理由的情况：**
```
"以后可能进欧洲"
"别的SaaS都有多语言"
"技术上不难"
"看起来更国际化"
```

**CPO判断：**
> 语言不是功能，是市场承诺。在你没承诺一个新市场之前，不要承诺一种新语言。

---

## ✅ 实施清单（CTO可直接执行）

### Phase 1 - 核心文件创建（1天）

- [ ] 创建文件结构（6个文件）
  ```
  content/copy.types.ts
  content/copy.en.ts
  content/copy.zh.ts
  components/landing/LanguageSwitch.tsx
  components/landing/Header.tsx
  components/landing/Section.tsx
  components/landing/LandingPage.tsx
  app/(marketing)/page.tsx
  ```

- [ ] 复制粘贴所有代码
- [ ] 验证TypeScript编译通过
- [ ] 本地预览运行

---

### Phase 2 - 内容替换（半天）

- [ ] 替换Logo（Header中的placeholder）
- [ ] 添加真实地下室场景图片
  - 要求：暗、乱、湿的真实工地
  - 位置：Basement Section
  
- [ ] 添加Smart Trace UI截图
  - 要求：建议弹窗，不要"自动归档"
  - 位置：Smart Trace Section

---

### Phase 3 - 测试验收（半天）

**功能测试：**
- [ ] 语言切换不刷新页面
- [ ] 语言选择保存到localStorage
- [ ] 桌面端Header正常显示
- [ ] 移动端Menu正常展开
- [ ] Scroll按钮平滑滚动

**内容测试：**
- [ ] 所有英文文案正确显示
- [ ] 所有中文文案正确显示
- [ ] 5个section锚点链接正常工作

**响应式测试：**
- [ ] 桌面端（≥1024px）布局正常
- [ ] 平板端（768-1024px）布局正常
- [ ] 移动端（<768px）布局正常

---

### Phase 4 - 部署上线（1天）

- [ ] 合并代码到main分支
- [ ] 部署到Vercel/生产环境
- [ ] 验证生产环境运行正常
- [ ] 设置域名DNS
- [ ] 添加Google Analytics（可选）

---

## 🎨 设计建议（给设计/前端）

### 视觉素材要求

#### 1. Basement Scene（必需）

**要求：**
```
✅ 真实的地下室照片
✅ 暗、乱、湿的工地环境
✅ 能看出"信号不好"的场景

❌ 不要干净样板间
❌ 不要Photoshop生成
❌ 不要过度美化
```

---

#### 2. Smart Trace Preview（必需）

**要求：**
```
✅ 建议弹窗UI截图
✅ 清楚显示"Confirm"按钮
✅ 强调"等待用户确认"

❌ 不要显示"自动归档"
❌ 不要显示进度条
❌ 不要AI/智能相关视觉
```

---

### 文案呈现原则

**Hero到第二屏的转场：**
```
Hero: 100vh高度
Scroll按钮 → 平滑滚动到Basement
给用户"进入工地"的代入感
```

---

**三个失败案例：**
```
不要做成"功能介绍"
要像"事故复盘"

标题是情绪
正文是原因
JSS是对策
```

---

**Smart Trace部分：**
```
不要动画太花
一个"建议弹窗+确认按钮"的静态mock就够

重点是：
"系统在等你"的感觉
```

---

## 📊 成功指标

### 核心转化指标

**3秒测试：**
```
用户能否在3秒内理解：
"这不是CompanyCam，这是工地相机"
```

**信任感指标：**
```
失败案例部分停留时间
Smart Trace部分点击率
Why Switch部分引用率
```

---

### A/B测试建议（Phase 2）

**测试变量：**
- Hero标题（2个版本）
- CTA文案（2个版本）
- Basement场景图（真实 vs 干净）

---

## 💬 CPO最后的话

### 给CEO的核心提醒

**这套官网的战略意义：**

> 你不是在"卖功能"，你是在"站队"。Hero到Why Switch整条线，都在反复说一件事："我们站在工地这一边，不站在云端那一边。"

---

### 给CTO的实施建议

**技术选型正确性：**
```
✅ 静态文案文件（易维护）
✅ TypeScript类型安全
✅ 不依赖i18n框架（减复杂度）
✅ localStorage记忆语言（不自作主张）
```

**这个方案的优点：**
- 最快上线（1-2天）
- 不过度工程化
- 未来扩展不返工

---

### 给市场团队的使用建议

**文案使用原则：**
```
Hero/H2：给第一次来的人
"No signal? Still counts"：给真正干活的人
"No silent automation"：给被CompanyCam坑过的人
```

**不要做的事：**
- ❌ 不要温和化失败案例
- ❌ 不要弱化对比立场
- ❌ 不要加营销噪音

---

## 📝 一句话PR说明

```
Marketing website implementation:
- 5-screen narrative (Hero → Basement → Failures → Smart Trace → Why Switch)
- English/中文 dual-language support with localStorage persistence
- Offline-first product positioning vs cloud-first competitors
- Mobile-responsive, no-refresh language switching
- Ready for production deployment
```

---

**文档版本：** v1.0  
**创建人：** CPO + COO  
**审核人：** CEO  
**执行人：** CTO + 前端团队  
**生效日期：** 2026-02-07  
**预计完成：** 2-3天

---

我们站在工地这一边，不站在云端那一边！🎯
