# PlanSnap 产品设计蓝图 - CDO 整理版

> **产品：** PlanSnap - 2D Floor Plan Tool  
> **目标用户：** General Contractor / Renovation Contractor  
> **平台：** Web (Desktop-first) + iPad PWA  
> **Sprint：** Sprint 2-3  
> **整理人：** CDO  
> **日期：** 2026-02-02

---

## 🎯 产品定位（必须统一认知）

### 一句话定义

> **PlanSnap 是一个按比例、基于网格、用"线"画的 2D Floor Plan 工具，**  
> **服务真实施工沟通，而不是营销展示。**

---

### PlanSnap 不是

```
❌ CAD
❌ SketchUp
❌ 图片拼装工具
❌ 自动生成墙体/房间的 AI 工具
```

---

## 📐 核心设计哲学（4条铁律）

### 所有功能必须同时满足：

**1. 线优先（Line-first）**

```
墙、房间、结构都由线构成
不是对象，是线段的组合
```

**2. 网格磁力优先（Grid + Snap）**

```
不靠"智能猜测"
依靠网格和吸附点
```

**3. 比例真实（Scaled Drawing）**

```
所画即所量
显示真实单位（ft/in）
```

**4. 快于 CAD，准于手画**

```
10 分钟出图是硬指标
不需要教程就能上手
```

---

## 👥 目标用户与场景

### 核心用户

- **General Contractor**
- **Renovation Contractor**
- **Owner / PM**（懂施工但不画 CAD）

### 核心场景

1. 给客户解释 layout
2. Permit 前方案讨论
3. 现场快速改图
4. 作为后续 IsoSnap / JSS 的前置工具

---

## 💻 平台优先级

| 平台               | 定位        | 优先级   |
| ---------------- | --------- | ----- |
| Desktop / Laptop | 主力创作平台    | 🔴 P0 |
| iPad             | 工地创作 / 讲解 | 🟡 P1 |
| 手机               | 查看 / 轻编辑  | 🟢 P2 |

---

## ⌨️ 桌面端交互规范（核心）

### 快捷键体系（必须支持）

**快捷键优先于按钮**

| 功能   | 快捷键             | 行为     |
| ---- | --------------- | ------ |
| 画线   | **L**           | 连续画线   |
| 移动画布 | **H**           | 拖动画布   |
| 尺寸标注 | **D**           | 选择两点标注 |
| 辅助线  | **T**           | 非实体线   |
| 选择   | **Esc / Space** | 回到选择   |

**注意：**

```
⚠️ 不要求 100% 复刻 SketchUp
但逻辑必须一致
```

---

### 线条与节点模型（基础数据结构）

**Line（线段）：**

```typescript
interface Line {
  id: string;
  startNodeId: string;  // 起点 Node
  endNodeId: string;    // 终点 Node
  length: number;       // 真实单位（feet）
  type: 'wall' | 'guide';
}
```

**Node（节点）：**

```typescript
interface Node {
  id: string;
  x: number;
  y: number;
  connectedLines: string[];  // 连接的线段
  type: 'endpoint' | 'intersection' | 'midpoint';
}
```

**Guide Line（辅助线）：**

```
不参与实体计算
可删除 / 隐藏
用于辅助对齐
```

**关键规则：**

```
❗墙厚不自动生成
❗墙厚 = 两条平行线
❗不自动闭合房间
❗不自动识别"你在画墙"
```

---

### 网格与吸附（Grid & Snap）

**必须支持：**

**Grid 控制：**

- Grid on/off
- Grid size:
  - 1 ft
  - 6 in
  - 1 in

**Snap 类型（优先级顺序）：**

1. Grid
2. Endpoint
3. Midpoint
4. Intersection

**明确禁止：**

```
❌ 自动闭合房间
❌ 自动识别"你在画墙"
```

---

## 📏 比例与画布系统

### 默认设置

```
比例：1/4" = 1'-0"
单位：Imperial（ft / in）
画布：无限画布（创作阶段）
```

---

### 导出规则

**导出时：**

- 自动检测内容 bounding box
- 提示：
  - 推荐纸张（A4 / Letter）
  - 推荐比例
- 默认最大有效宽度：≈ 44 ft

**系统提示示例：**

```
"Current drawing fits A4 @ 1:50"
```

**原则：**

> 用户不需要懂制图，但系统要"像懂制图一样提醒他"

---

## 📐 尺寸标注（D 工具）

### 行为

1. 点击两点生成尺寸
2. 自动吸附线段
3. 显示格式：`12'-6"`

### 编辑

- 双击可改文字
- **不强制回写几何**（非 CAD）

---

## ✏️ 编辑能力（MVP 必须）

**基础编辑：**

- [ ] Select（单选 / 框选）
- [ ] 移动线段
- [ ] 拉伸端点
- [ ] 删除
- [ ] Undo / Redo（≥ 20 步）

---

## 📱 iPad 交互规范

### 手势

| 操作   | 手势                |
| ---- | ----------------- |
| 移动画布 | 两指拖动              |
| 缩放   | 双指捏合              |
| 画线   | Apple Pencil / 单指 |
| 选择   | 点选                |
| 框选   | 长按拖拽              |

### UI 原则

```
无快捷键依赖
左侧工具栏
当前工具强高亮
```

---

## 📲 手机端（严格克制）

### 允许

```
✅ 查看
✅ 缩放 / 平移
✅ 删除 / 移动简单线段
✅ 查看尺寸
```

### 不做

```
❌ 从零画复杂平面
❌ 高密度标注
```

---

## 🚫 明确不在 MVP 的内容（禁止）

```
❌ 自动生成墙体厚度
❌ 智能房间识别
❌ Code / Permit 校验
❌ 3D / Isometric
❌ 材料清单 / BOM
❌ 多人协作
```

---

## ✅ 成功验收标准（CPO 口径）

> **PlanSnap v1 合格的标志是：**

```
一个做装修的人，
不用看教程，
10 分钟内画出一张"敢发给客户/Inspector"的平面图。
```

---

## 🔗 与 IsoSnap / JSS 的关系

**边界声明：**

```
PlanSnap 只负责 2D 平面表达
不做任何 isometric / pipe
Project ID 结构与 IsoSnap 兼容
为 JSS 预留入口，不内嵌
```

---

## 🎨 Figma 页面结构蓝图

### 结构原则

```
页面少、层级浅、状态清楚
不做复杂路由，不做多产品混合
```

---

## 📋 Figma 页面清单（共11页）

### Page 0 · Design System（先画这个）⭐

**为什么先画这个：**

> ⚠️ 没这个，后面一定乱

**Components（组件）：**

```
✅ Grid（浅色 / 深色）
✅ Line（默认 / selected / hover）
✅ Node（endpoint / intersection）
✅ Dimension Label
✅ Guide Line（虚线）
✅ Selection Box
✅ Cursor States（draw / pan / select）
```

**Tokens（设计规范）：**

```
✅ Line thickness（px）
✅ Snap radius
✅ Grid spacing
✅ Font size（dimension / label）
✅ Color:
   - Primary line
   - Selected
   - Guide
   - Grid
```

**目标：**

> 这页是技术 & 设计共用语言

---

### Page 1 · App Shell（整体框架）

**Frame:** Desktop 1440 × 900

**Layout：**

```
┌───────────────────────────────────────────┐
│ Top Bar (File / Export / Undo / Redo)     │
├───────┬───────────────────────────────────┤
│ Tool  │                                   │
│ Bar   │        Canvas / Grid Area         │
│       │                                   │
│       │                                   │
└───────┴───────────────────────────────────┘
```

**必须标注：**

- Canvas 是无限画布
- Grid 默认开启
- 无 modal 打断画图

---

### Page 2 · Tool Bar（左侧）

**Tools（顺序不能乱）：**

```
1. Select
2. Line (L)
3. Dimension (D)
4. Guide (T)
5. Pan (H)
```

**States：**

- Active（高亮）
- Hover
- Disabled

**设计原则：**

```
⚠️ 不要 icons 太 fancy
👉 施工工具感 > 设计感
```

---

### Page 3 · Line Drawing Flow（核心）⭐⭐⭐

**这是最重要的页面！**

**7 个状态帧（必须全部画出）：**

```
Frame 0: Idle（默认状态）
Frame 1: Tool Armed（按 L 或点 Line）
Frame 2: Hover Snap（未点击）
Frame 3: First Click（确定起点）
Frame 4: Drag Preview（拖拽预览）
Frame 5: Second Click（确定终点）
Frame 6: Commit & Continue（连续画线）
Frame 7: Cancel / Undo（安全网）
```

**详细交互见下一节。**

---

### Page 4 · Dimension Tool（D）

**Flow：**

```
1. Click point A
2. Click point B
3. Dimension appears
4. Double click → edit text
```

**States：**

- Default
- Selected
- Hover
- Editing

**重要：**

```
⚠️ Dimension 不影响 geometry
```

---

### Page 5 · Grid & Snap Behavior

**Grid：**

- On / Off
- Size selector（1ft / 6in / 1in）

**Snap Types（视觉表达）：**

- Grid snap
- Endpoint snap
- Midpoint snap
- Intersection snap

**设计原则：**

```
👉 用小 icon / glow 表达
👉 不要文字提示干扰
```

---

### Page 6 · Selection & Edit

**Selection：**

- Single select
- Box select（虚线框）

**Edit Handles：**

- Endpoint drag
- Whole line move

**States：**

- Selected line color
- Node highlight

---

### Page 7 · Export Flow

**Export Panel（右侧或 top modal）：**

```
Format: PNG / PDF
Paper size: A4 / Letter
Scale preview（1/4"=1'）
```

**System Hint：**

```
"Current drawing fits A4 @ 1:50"
```

**目标：**

> 这页体现工程专业感

---

### Page 8 · iPad Layout

**Frame:** iPad Pro 11"

**Differences：**

- No keyboard hints
- Tool bar = icons only
- Larger snap radius

**Gestures Callout：**

- Two-finger pan
- Pinch zoom
- Pencil draw

---

### Page 9 · Mobile (View-first)

**Frame:** iPhone

**功能：**

- Canvas only
- Minimal tool bar
- Edit mode warning

**提示：**

```
"Mobile is for viewing & light edits"
```

---

### Page 10 · Explicitly NOT Doing（设计对齐页）

**非 UI，而是团队对齐页**

**List：**

```
❌ No wall objects
❌ No auto room
❌ No isometric
❌ No pipe tools
❌ No code check
```

**目标：**

> 防止设计"好心办坏事"

---

### Page 11 · Keyboard Cheat Sheet（加分项）

**Small overlay mock：**

```
L  Draw Line
H  Pan
D  Dimension
T  Guide
Esc Select
```

---

## 📐 Page 3 详解 - Line Drawing Flow（核心交互）

### 总览：画一根线 = 7 个状态帧

```
Idle → Tool Armed → Hover Snap → First Click
→ Drag Preview → Second Click → Commit / Continue → (Cancel/Undo)
```

**任何时刻都必须可：**

- Esc 取消
- Undo 回到上一步

---

### Frame 0 · Idle（默认状态）

**视觉：**

```
Cursor: Arrow
Grid: 可见（淡）
所有 node: 不可见
```

**行为：**

```
Mouse move: 无效果
Click: 只会 select（如果点到已有线）
```

---

### Frame 1 · Tool Armed（按 L 或点 Line）

**视觉：**

```
Cursor → Crosshair / Pencil
Tool Bar: Line 高亮
Grid snap 激活
Snap radius 可见（debug 可选）
```

**行为：**

```
Mouse move: 开始检测 snap 点
尚未点击前不生成任何几何
```

---

### Frame 2 · Hover Snap（未点击）

**触发：**

```
Cursor 靠近以下任一对象：
- Grid intersection
- Existing line endpoint
- Line midpoint
- Line intersection
```

**视觉：**

```
最近合法 snap 点高亮（小实心圆）
Cursor magnet icon（可选）
Tooltip（可选，开发模式）:
  - "Snap: Grid"
  - "Snap: Endpoint"
```

**行为：**

```
Snap 点随 cursor 实时变化
不允许自由角度（始终 snapped）
```

---

### Frame 3 · First Click（确定起点）

**触发：**

```
Mouse down + up（单击）
```

**视觉：**

```
起点 node 固定
Node 变为实心
画线模式进入 Active Drawing
```

**行为：**

```
记录 startNode（x, y）
系统进入 Drag Preview 状态
```

**关键规则：**

```
⚠️ 起点必须是 snap 点
⚠️ 不允许"空中起点"
```

---

### Frame 4 · Drag Preview（拖拽预览）⭐

**这是最重要的一帧！**

**视觉：**

```
从起点到 cursor 的虚线
虚线只能沿：
  - 水平
  - 垂直

当前长度浮窗：
  - "12'-6""
```

**Snap 行为（优先级）：**

```
1. Shift 键 → 强制水平 / 垂直
2. 默认 → 最近轴向 snap
3. Grid / endpoint / midpoint
```

**行为：**

```
Mouse move:
  - 更新 preview line
  - 更新长度显示
不生成实体线
```

---

### Frame 5 · Second Click（确定终点）

**触发：**

```
Mouse click
```

**视觉：**

```
虚线 → 实线
终点 node 生成
Line 成为默认选中状态（淡高亮）
```

**行为：**

```
创建 Line object:
  - startNode
  - endNode
  - length（真实单位）

如果终点在已有 node:
  - 合并 node（非常重要）
```

---

### Frame 6 · Commit & Continue（连续画线）

**默认行为（非常关键）：**

```
画线不会自动退出
```

**视觉：**

```
当前终点作为新起点
Cursor 仍为 Line 模式
新 preview 虚线自动出现
```

**行为：**

```
可直接继续画下一段
Esc / Space → 退出 Line Tool
Right click（可选）→ Finish
```

---

### Frame 7 · Cancel / Undo（安全网）

**Esc：**

```
如果还没 Second Click:
  - 清除 preview
  - 回到 Tool Armed

如果刚 commit:
  - 撤销上一段
```

**Undo：**

```
撤销整段 line
Node 若无连接则删除
```

---

## 🧠 关键规则（必须写在 Figma 注释里）

### 规则 1：线永远连接 node

```
不允许视觉重叠但数据不连
```

### 规则 2：所有长度是真实比例

```
显示 ft / in
不允许 px-only
```

### 规则 3：不做自动闭合

```
用户画什么就是什么
```

### 规则 4：画线速度 > 智能

```
不弹窗
不猜意图
```

---

## 🖥 Desktop vs iPad 差异

### Desktop

```
Mouse click
Shift 锁方向
Keyboard Esc / L
```

### iPad

```
Pencil tap = click
长按 = snap 强制
无 Shift
工具栏退出 Line
```

---

## 🚫 明确禁止的交互（要写给设计）

```
❌ 自动生成墙厚
❌ 自动变成房间
❌ 自动修正不直的线
❌ 自由角度（非 0 / 90）
```

---

## ✅ 交付检查清单（给设计师）

- [ ] 是否只用"线"表达？
- [ ] 是否任何一步不需要教程？
- [ ] 是否像施工工具而不是设计软件？
- [ ] 是否 10 分钟能出图？

---

## 🎯 CPO 验收标准（设计）

> **一个老施工，用鼠标连点 6 下，**  
> **能画出一个房间轮廓，**  
> **不需要思考"我现在在哪个模式"。**

---

## 🎯 CPO 验收标准（产品）

> **一个做装修的人，**  
> **不用看教程，**  
> **10 分钟内画出一张"敢发给客户/Inspector"的平面图。"**

---

## 📊 设计优先级

| 页面                    | 优先级   | 工作量  | 说明        |
| --------------------- | ----- | ---- | --------- |
| Page 0: Design System | 🔴 P0 | 2天   | 先做，否则后面乱  |
| Page 1: App Shell     | 🔴 P0 | 1天   | 整体框架      |
| Page 2: Tool Bar      | 🔴 P0 | 1天   | 核心工具      |
| Page 3: Line Drawing  | 🔴 P0 | 3天   | 最重要！7个状态帧 |
| Page 4: Dimension     | 🟡 P1 | 1天   | 标注功能      |
| Page 5: Grid & Snap   | 🟡 P1 | 1天   | 交互细节      |
| Page 6: Selection     | 🟡 P1 | 1天   | 编辑能力      |
| Page 7: Export        | 🟡 P1 | 1天   | 导出流程      |
| Page 8: iPad          | 🟢 P2 | 1天   | iPad 适配   |
| Page 9: Mobile        | 🟢 P2 | 0.5天 | 移动端       |
| Page 10: NOT Doing    | 🟢 P2 | 0.5天 | 团队对齐      |
| Page 11: Cheat Sheet  | 🟢 P2 | 0.5天 | 加分项       |

**总计：** 13-14天设计工作量

---

## 💡 给设计师的关键提醒

### 1. 设计风格

```
施工工具感 > 设计感
干净 > 花哨
专业 > 美观
```

### 2. 不要做的事

```
❌ 不要试图"智能化"
❌ 不要自动生成任何几何
❌ 不要弹窗打断画图流程
❌ 不要 fancy 的 icons
```

### 3. 核心原则

```
快速 > 完美
清晰 > 炫酷
可预测 > 智能
```

---

## 🔧 给 CTO 的数据结构建议

### 核心数据模型

```typescript
// /packages/plan-snap/src/types/index.ts

// 节点
export interface Node {
  id: string;
  x: number;        // Grid 坐标
  y: number;
  realX: number;    // 真实单位（feet）
  realY: number;
  connectedLines: string[];
  type: 'endpoint' | 'intersection' | 'midpoint';
}

// 线段
export interface Line {
  id: string;
  startNodeId: string;
  endNodeId: string;
  length: number;   // 真实长度（feet）
  angle: 0 | 90;    // 只允许水平或垂直
  type: 'wall' | 'guide';
  thickness: number; // 显示粗细
}

// 尺寸标注
export interface Dimension {
  id: string;
  pointA: { x: number; y: number };
  pointB: { x: number; y: number };
  displayText: string;  // "12'-6""
  editable: boolean;
}

// 项目数据
export interface PlanData {
  nodes: Map<string, Node>;
  lines: Map<string, Line>;
  dimensions: Map<string, Dimension>;
  grid: {
    size: 1 | 6 | 12;  // 1ft / 6in / 1in
    visible: boolean;
  };
  scale: {
    ratio: string;      // "1/4\" = 1'-0\""
    unit: 'imperial';
  };
}
```

---

## 🚀 实施建议

### Phase 1: 设计（Week 1-2）

**Week 1：**

- Day 1-2: Page 0 Design System
- Day 3: Page 1 App Shell
- Day 4: Page 2 Tool Bar
- Day 5: Page 3 Line Drawing（开始）

**Week 2：**

- Day 1-3: Page 3 Line Drawing（完成）
- Day 4: Page 4-7（Dimension, Grid, Selection, Export）
- Day 5: Page 8-11（iPad, Mobile, NOT Doing, Cheat Sheet）

---

### Phase 2: 开发（Week 3-4）

**参考 Sprint 1 技术任务清单中的 PlanSnap 部分**

---

## 📎 相关文档

**已有文档：**

- Sprint 1 技术任务清单（包含 PlanSnap Task C1-C4）
- PlanSnap & IsoSnap 双产品线战略决策
- 产品边界不可越界清单

**待创建文档：**

- [ ] PlanSnap 用户手册（给 SketchUp 用户的 5 分钟上手指南）
- [ ] "PlanSnap 为什么不做 X"（团队统一认知）
- [ ] PlanSnap 测试用例清单

---

## 🎓 核心理念（牢记）

### CPO 的产品哲学

> **"快于 CAD，准于手画，10 分钟出图"**

### 设计师要记住

> **"施工工具感 > 设计感"**

### 开发者要记住

> **"线优先，网格磁力优先，比例真实"**

### 验收者要记住

> **"一个老施工，用鼠标连点 6 下，能画出一个房间轮廓"**

---

**版本：** v1.0  
**创建时间：** 2026-02-02  
**状态：** ✅ 已确认，待设计  
**优先级：** 🔴 P0 - Sprint 2-3

---

**这是一份可以直接交给设计师和CTO的产品蓝图。设计师按照 Figma 页面结构画，CTO 按照数据模型开发。** 🚀
