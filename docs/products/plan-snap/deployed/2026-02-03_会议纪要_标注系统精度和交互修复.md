# 会议纪要 - 标注系统精度和交互修复

> **会议时间：** 2026-02-03  
> **会议类型：** Bug修复 + 功能增强  
> **参与人员：** CPO、CDO  
> **优先级：** 🔴 P0 - Critical  
> **状态：** ✅ 已确定方案

---

## 📋 会议背景

### 问题发现

**CPO发现的问题：**
```
标注显示：3'-0"
实际尺寸：36-1/4"
```

**问题截图：**
- 右侧标注数值不准确
- 标注不能像SketchUp那样选择方向和拉伸

---

### 问题分析

**CPO指出，这暴露了两个独立问题：**

1. **标注数值显示被"精度/格式化"吃掉了**
   - 36-1/4" 被显示成 3'-0"
   - 1/4" 的分数部分丢失

2. **标注交互不够 CAD/SketchUp**
   - 不能选方向
   - 不能拉伸（offset/翻边）
   - 放置后不可调整

---

## 🎯 核心决策

### 一、精度问题根因和修复

#### 问题1A：显示精度四舍五入错误

**根因：**
```
36-1/4" = 36.25" = 3'-0 1/4"

如果看到 3'-0"，说明：
- 显示精度在"四舍五入到整英寸（或1/2"）"
- 0.25" 被舍掉了
- formatter有逻辑：inchFraction < threshold => 0（错误）
```

---

#### 问题1B：标注测量拿错单位

**可能原因：**
```
把 world mm → feet 的过程做了 Math.round()
或用"格子步长"替代真实距离
或从像素/grid index反算
```

---

#### ✅ 正确做法（强制）

**标注数值必须永远来自：**
```
世界坐标的真实几何距离（mm）
  ↓
仅在UI层格式化成英制/公制
```

**绝对不能从：**
```
❌ 像素
❌ grid index
❌ 显示文本反算
```

---

### 二、标注交互：对标SketchUp

#### SketchUp的"Aligned Dimension"心智模型

**三段式交互：**
```
1. 选两点（或一条边）
2. 鼠标移动决定标注放在哪一侧（上/下/左/右）
3. 第三次点击把标注放下
```

**放下后：**
```
能拖动标注线偏移（不改变测量点）
```

---

#### PlanSnap应该的实现

**Dimension Tool 状态机（推荐）：**

**Step 1: Pick A**
```
吸附点/端点/交点/guide
显示预览点
```

**Step 2: Pick B**
```
选择第二个点
显示测量距离预览
```

**Step 3: Place**
```
移动鼠标选择"方向 + 偏移距离"
click 放置
```

---

#### 放置后的增强功能

**拖动标注线：**
```
拖动标注线 = 改 offsetMm
不改变测量点A和B
```

**快捷键翻转：**
```
F 或 X = 翻转到另一侧（flip）
```

**偏移方向锁定：**
```
Shift = 偏移方向锁定为"垂直于测量线"（防乱飘）
```

**输入偏移值：**
```
允许输入偏移距离：6" / 150mm
回车确认
复用分数输入解析器
```

---

### 三、显示精度设置（必须加）

#### 为什么必须加

**CPO明确指出：**
> "既然你要做到施工级，英制必须支持：1/2"、1/4"、1/8"、1/16"。不然永远吵不完。"

---

#### UI设计（最简单就够）

**位置：**
```
右上角单位旁边
```

**选项：**
```
Precision: 1/16" （下拉菜单）

可选值：
- 1" (整英寸)
- 1/2"
- 1/4"
- 1/8"
- 1/16" (建议默认)
```

---

#### Formatter规则（强制）

**内部存储：**
```
mm (double/int都行)
```

**显示格式：**
```
按 precision rounding
```

**示例：**
```
36.25" 在 1/16 精度下必须显示为：
  3' 0 1/4"
或
  3'-0 1/4"

（选一种统一）
```

---

## 📋 技术方案要点

### 数据模型

```typescript
type Dimension = {
  id: string
  kind: 'dimension'
  
  // Source of truth（世界坐标）
  A_world: Pt
  B_world: Pt
  
  // 偏移和方向
  offsetMm: number        // 标注线到测量线的距离
  orientationMode: 'auto' | 'horizontal' | 'vertical' | 'aligned'
  
  // 可选
  flipSide?: boolean      // 是否翻转到另一侧
  locked?: boolean
}
```

---

### 渲染逻辑

```typescript
function renderDimension(dim: Dimension) {
  // 1. 计算方向向量
  const dir = normalize(sub(dim.B_world, dim.A_world))
  
  // 2. 计算法线（垂直方向）
  const normal = perp(dir)
  if (dim.flipSide) {
    normal = mul(normal, -1)
  }
  
  // 3. 计算标注线位置
  const dimLineA = add(dim.A_world, mul(normal, dim.offsetMm))
  const dimLineB = add(dim.B_world, mul(normal, dim.offsetMm))
  
  // 4. 绘制extension lines（从A/B拉到标注线）
  drawLine(dim.A_world, dimLineA, { dash: true })
  drawLine(dim.B_world, dimLineB, { dash: true })
  
  // 5. 绘制标注线
  drawLine(dimLineA, dimLineB, { withArrows: true })
  
  // 6. 计算并显示文本
  const lenMm = distance(dim.A_world, dim.B_world)
  const text = formatLength(lenMm, unitSystem, precision)
  drawText(text, midpoint(dimLineA, dimLineB))
}
```

---

### 文本格式化

```typescript
function formatLength(
  lenMm: number, 
  unitSystem: 'imperial' | 'metric',
  precision: Precision
): string {
  if (unitSystem === 'metric') {
    // 公制简单
    if (lenMm < 1000) {
      return `${Math.round(lenMm)} mm`
    } else {
      return `${(lenMm / 1000).toFixed(2)} m`
    }
  }
  
  // 英制：必须支持分数
  const inches = lenMm / 25.4
  
  // 分解为 feet + inch + fraction
  const feet = Math.floor(inches / 12)
  const remainingInches = inches - feet * 12
  
  const { whole, fraction } = roundToFraction(
    remainingInches, 
    precision
  )
  
  // 格式化
  if (feet > 0) {
    if (fraction) {
      return `${feet}' ${whole} ${fraction}"`
    } else if (whole > 0) {
      return `${feet}' ${whole}"`
    } else {
      return `${feet}'`
    }
  } else {
    if (fraction) {
      return `${whole} ${fraction}"`
    } else {
      return `${whole}"`
    }
  }
}
```

---

### 精度圆整

```typescript
type Precision = 1 | 0.5 | 0.25 | 0.125 | 0.0625  // 1", 1/2", 1/4", 1/8", 1/16"

function roundToFraction(
  inches: number,
  precision: Precision
): { whole: number; fraction: string | null } {
  // 圆整到指定精度
  const rounded = Math.round(inches / precision) * precision
  
  // 分解整数和分数部分
  const whole = Math.floor(rounded)
  const frac = rounded - whole
  
  if (frac < 0.001) {
    return { whole, fraction: null }
  }
  
  // 转换为分数字符串
  const fraction = decimalToFraction(frac, precision)
  
  return { whole, fraction }
}

function decimalToFraction(
  decimal: number,
  precision: Precision
): string {
  // 根据precision确定分母
  const denominator = Math.round(1 / precision)
  const numerator = Math.round(decimal * denominator)
  
  if (numerator === 0) return null
  
  // 简化分数
  const gcd = greatestCommonDivisor(numerator, denominator)
  const num = numerator / gcd
  const den = denominator / gcd
  
  return `${num}/${den}`
}
```

---

## ✅ 完成定义（DoD）

### 数值准确性 DoD

- [ ] **标注值来自世界坐标真实距离（mm）**
  - 与grid/zoom无关
  - 不从像素反算

- [ ] **precision=1/16" 时，36-1/4" 必须显示**
  - `3'-0 1/4"` 或等价格式
  - 不能显示为 `3'-0"`

- [ ] **改precision只影响显示，不改几何**
  - 内部存储仍是mm
  - 只是显示方式不同

---

### 交互 DoD

- [ ] **选A、选B、第三次click放置**
  - 三段式交互
  - 每步都有明确反馈

- [ ] **鼠标移动可决定标注方向**
  - 两侧切换
  - 实时预览

- [ ] **放置后可拖拽偏移**
  - 不改变A/B测量点
  - 只改offsetMm

- [ ] **支持输入偏移距离**
  - 复用分数输入解析器
  - 支持 6" / 150mm

- [ ] **快捷键翻转**
  - F 或 X 翻转侧边
  - Shift 锁定垂直方向

---

### 视觉 DoD

- [ ] **标注线可拉伸**
  - leader/extension lines自动更新
  - 跟随offsetMm变化

- [ ] **文本永远不被线压住**
  - 必要时自动避让
  - 保持可读性

- [ ] **精度设置可见**
  - 右上角显示
  - 实时生效

---

## 🔧 关键实现点（避免再出类似bug）

### 1. Source of Truth

**Dimension的唯一真相来源：**
```typescript
{
  A_world: Pt      // 世界坐标
  B_world: Pt      // 世界坐标
  offsetMm: number // mm
  orientationMode: string
}
```

**不要存储：**
```
❌ 显示文本（"3'-0 1/4"）
❌ 屏幕坐标
❌ Grid索引
```

---

### 2. 渲染时计算

**每次渲染时：**
```typescript
// 1. 方向向量
dir = normalize(B - A)

// 2. 法线（根据鼠标/flipSide决定正负）
normal = perp(dir)

// 3. 标注线位置
dimLine = (A + normal*offset) to (B + normal*offset)

// 4. Extension lines
从 A/B 拉到 dimLine

// 5. 文本
lenMm = distance(A, B)
text = format(lenMm, unitSystem, precision)
```

---

### 3. 格式化分离

**关键原则：**
```
存储（内部）：mm
显示（UI）：按unitSystem和precision格式化
```

**不要混淆：**
```
❌ 存储时就转成英制
❌ 存储显示文本
```

---

## 🎨 UI/UX规范

### 精度选择器

**位置：**
```
右上角，单位选择旁边
```

**样式：**
```
[英制 ▼] [1/16" ▼]
```

**下拉选项：**
```
1"       (整英寸)
1/2"
1/4"
1/8"     (推荐默认)
1/16"    (施工级精度)
```

---

### 标注交互反馈

#### Step 1: Pick A
```
光标：十字
提示："选择第一个点"
吸附：endpoint/midpoint/intersection
```

#### Step 2: Pick B
```
光标：十字
提示："选择第二个点"
预览：虚线连接A-B，显示距离
```

#### Step 3: Place
```
光标：标注图标
提示："选择标注位置"
预览：
  - 完整标注线
  - 文本
  - 跟随鼠标切换侧边
```

---

### 放置后的编辑

**拖动标注线：**
```
hover：标注线高亮
drag：偏移距离实时更新
```

**快捷键：**
```
F/X：翻转到另一侧
Shift：锁定垂直方向
```

**输入偏移：**
```
选中标注 → 输入 "6"" → Enter
标注线移动到6"偏移
```

---

## 🧪 测试用例

### TC-D001: 精度显示正确性

**步骤：**
```
1. 设置precision = 1/16"
2. 创建36-1/4"的标注
3. 观察显示
```

**期望：**
```
显示：3'-0 1/4" 或 3' 0 1/4"
不是：3'-0" 或 3'
```

---

### TC-D002: 切换精度不改几何

**步骤：**
```
1. 创建36-1/4"的标注
2. 切换precision从1/16"到1/2"
3. 再切回1/16"
```

**期望：**
```
1/2"时显示：3'-0" (四舍五入)
切回1/16"时显示：3'-0 1/4" (原值)
几何从未改变
```

---

### TC-D003: 三段式交互

**步骤：**
```
1. 选点A
2. 选点B
3. 移动鼠标
4. 观察预览
5. Click放置
```

**期望：**
```
鼠标上方：标注预览在上侧
鼠标下方：标注预览在下侧
click后：标注固定在该位置
```

---

### TC-D004: 拖动偏移

**步骤：**
```
1. 创建标注（offsetMm=50）
2. 拖动标注线
3. 观察变化
```

**期望：**
```
标注线偏移改变
A/B测量点不变
文本数值不变
```

---

### TC-D005: 公制/英制切换

**步骤：**
```
1. 创建标注（1000mm）
2. 英制显示
3. 切换到公制
```

**期望：**
```
英制：3'-3 3/8"
公制：1000 mm 或 1.00 m
几何不变
```

---

## 💬 CPO关键引用

### 关于精度问题

> "36-1/4" 被显示成 3'-0"，说明你们的显示精度在四舍五入到整英寸，0.25" 被舍掉了。这是典型的formatter错误。"

---

### 关于数据源

> "标注数值必须永远来自世界坐标的真实几何距离（mm），仅在UI层格式化成英制/公制。绝对不能从像素、grid index、显示文本反算。"

---

### 关于交互模型

> "SketchUp的'Aligned Dimension'心智模型是：选两点、鼠标移动决定标注放哪一侧、第三次点击放下。你们也应该做成三段式。"

---

### 关于精度设置

> "既然你要做到施工级，英制必须支持：1/2"、1/4"、1/8"、1/16"。不然永远吵不完。"

---

## 📋 行动项

### CTO立即执行（紧急修复）

**优先级：** 🔴 P0

**Phase 1: 精度修复（1天）**
- [ ] 修复formatter（支持1/16"精度）
- [ ] 确保数据源是world坐标mm
- [ ] 添加precision设置UI

**Phase 2: 交互增强（2天）**
- [ ] 实现三段式交互
- [ ] 添加拖动偏移功能
- [ ] 添加快捷键翻转

---

### CDO配合工作

**任务：**
- [ ] 创建完整技术方案
- [ ] 创建快速执行清单
- [ ] 更新决策日志
- [ ] 更新文档索引

---

### 测试验收

**验收标准：**
- [ ] 36-1/4" 正确显示为 `3'-0 1/4"`
- [ ] 三段式交互流畅
- [ ] 放置后可拖动偏移
- [ ] 精度切换不改几何

---

## 🔗 相关文档

**本次会议产出：**
1. 本会议纪要
2. 完整技术方案（待创建）
3. 快速执行清单（待创建）

**相关历史文档：**
1. 分数输入系统（DEC-005） - 复用解析器
2. 单位系统（DEC-000） - 统一mm存储
3. Tape工具（DEC-005） - 类似的offset逻辑

---

**会议记录人：** CDO  
**会议时间：** 2026-02-03  
**下次Review：** Phase 1完成后验收  
**状态：** ✅ 方案已确定，待执行

---

## 🎯 总结

这次会议暴露并解决了标注系统的两个核心问题：

**1. 精度丢失问题：**
- 根因：formatter四舍五入错误
- 解决：world坐标mm → UI格式化，支持1/16"精度

**2. 交互不专业问题：**
- 根因：缺少SketchUp式的三段式交互
- 解决：选A → 选B → 放置，支持拖动偏移

这是施工级CAD工具的基础能力，必须一次做对！🚀
