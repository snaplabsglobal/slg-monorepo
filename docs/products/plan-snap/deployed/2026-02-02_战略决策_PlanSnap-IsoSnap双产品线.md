# PlanSnap & IsoSnap 双产品线战略决策

> **会议时间：** 2026-02-02  
> **参与人：** CEO, COO, CPO  
> **整理人：** CDO (Chief Data Officer)  
> **决策类型：** 🔴 战略级 - 产品路线重大调整  
> **状态：** ✅ 已确认，待执行

---

## 📋 战略调整背景

### 从单一产品到双产品线

**之前：** ISO SNAP（单一产品）  
**现在：** PlanSnap + IsoSnap（双产品线）

**核心理念：**
> "共用技术栈 ≠ 共用产品心智模型"

---

## 🎯 战略目标（CPO + COO 共识）

### 产品目标
- **简单、快、好用**
- 不是一个复杂、谁都不爽的混合体
- 两款独立的"心智工具"

### 商业目标（COO）
- **练手"注册到收费"链条**
- PlanSnap：练"广度"（用户量大）
- IsoSnap：练"深度"（粘性高，付费意愿强）
- 为 JSS 做准备

---

## ⚠️ 核心原则（最高优先级）

### CPO 的底线

> **PlanSnap 和 IsoSnap 是两款"心智工具"，不是同一产品的两个模式。**

**禁止行为：**
```
❌ 任何尝试"合并交互逻辑 / 合并 UI / 合并操作方式"的行为
   → 视为产品风险
```

### COO 的洞察

> **这份清单不是在教 CTO 怎么写代码，而是在保命。**

**创业早期最容易犯的错误：**
```
"顺手加个功能" 
→ 产品变成谁都用不明白的"怪胎"
```

---

## 🏗️ 架构设计：刀刃与刀柄

### ✅ 可以共用的部分（"刀柄"）

**技术层（允许 & 鼓励）：**

| 组件 | 说明 | 优先级 |
|------|------|--------|
| Canvas / WebGL 渲染引擎 | 基础图形引擎 | 🔴 P0 |
| Snap / Grid / Node / Edge | 基础数据模型 | 🔴 P0 |
| IndexedDB / LocalStorage | 本地存储 | 🔴 P0 |
| PWA 架构 | 离线可用 | 🔴 P0 |
| PDF / PNG 导出 | 文件导出 | 🔴 P0 |
| Auth / License / Payment | 认证支付 | 🔴 P0 |
| Project / File 容器 | 为 JSS 准备 | 🟡 P1 |

**COO 补充：**
- 许可引擎必须第一天就写得足够通用
- 支持：全家桶 $49 或单品 $19

---

### ❌ 必须分离的部分（"刀刃"）

**交互层（硬隔离）：**

```
❌ 共享同一套 Tool Bar
❌ 共享同一套 Mouse Interaction
❌ 共享同一套快捷键逻辑
❌ 用 "mode / toggle" 切换 Plan / Iso

→ 两个产品 = 两套交互哲学
```

---

## 📱 产品 A：PlanSnap

### 🎯 产品定位

**目标用户：** 普通 contractor / owner  
**用途：** 2D 平面图工具  
**心智模型：** "快速表达 layout"

---

### ✅ PlanSnap 必须具备

**核心功能：**

1. **纯 2D 平面视角**
   - 俯视图（Top-down view）
   - 不涉及等轴或3D

2. **基础绘图元素**
   - 房间 / 墙体 / 门窗
   - 尺寸线
   - 简单标注

3. **交互方式**
   - 拖拽、拉伸、对齐
   - 直观的平面操作
   - 快速修改

4. **使用场景**
   - 给客户看
   - 申请 permit
   - 团队沟通

---

### ❌ PlanSnap 明确禁止

**以下功能绝对不能加：**

```
❌ 等轴（Isometric）视角
❌ 管径 / fitting / pipe angle
❌ 工艺级表达（vent / slope / code）
❌ 任何"工程级精度"计算
❌ Pipe-style 起点→方向→终点绘制
```

**CPO 底线：**
> **PlanSnap 不服务管工，不做施工图**

---

### 🎨 UI/UX 原则（COO 建议）

**界面风格：**
```
像 Notability 一样简洁
点开就能画墙
```

**用户体验：**
- 不能让用户觉得"这玩意儿太难"
- 降低学习曲线
- 立即上手

---

## 🔧 产品 B：IsoSnap

### 🎯 产品定位

**目标用户：** 管工 / 技工  
**用途：** Isometric 快速表达工具  
**心智模型：** "能看懂的施工草图"

---

### ✅ IsoSnap 必须具备

**核心功能：**

1. **固定等轴角度**
   - 30° / 45° 视角
   - 不可随意旋转

2. **Pipe 为第一类对象**
   - 不是"线"，是"管"
   - Node-based 连接
   - 必须可接续

3. **智能绘制**
   - 自动 snap 到合法方向
   - 起点 → 方向 → 长度 → 终点
   - 方向锁定（vertical / 45° left / 45° right）

4. **Fitting 概念**
   - wye / tee / 45 / 90
   - 管道连接件
   - 专业语义

5. **快速表达**
   - 给 Inspector 看
   - 给客户解释
   - 工地沟通

---

### ❌ IsoSnap 明确禁止

**以下功能绝对不能加：**

```
❌ 平面房间 / 墙体 / 门窗
❌ 拖拽式平面拉伸
❌ 多视角切换（顶视 / 侧视）
❌ Code 检查
❌ 材料统计 / BOM
❌ 自动算坡度 / vent 合法性
```

**CPO 底线：**
> **IsoSnap 是"表达工具"，不是"设计审核工具"**

**COO 补充（法律风险）：**
> **如果承诺 Code Check，AI 算错了要承担巨大法律责任（Liability）**

**当前定位：**
```
我们卖的是"更专业的画笔"
不是"电子审核员"
→ 极大降低法律门槛
```

---

### 🎨 UI/UX 原则（COO 建议）

**界面风格：**
```
像制图软件一样严谨
点开就是 30° 坐标系
```

**用户体验：**
- 专业感
- 精确性
- 工具感

---

## 🚫 Phase 2/3 功能（现在明确不做）

### 未经 CPO 批准，不进入代码库

**以下功能"顺手加"会破坏产品定位：**

```
❌ Code compliance 检查
❌ 自动 vent / slope 判断
❌ 材料清单 / 价格计算
❌ 多人协作
❌ 实时云同步
❌ JSS 深度集成 UI
❌ 2D 自动转换 ISO（COO 特别警告）
```

---

### COO 的"2D 转 ISO"风险警告

**诱惑：**
```
"我们能不能做个功能，自动把平面图转成等轴图？"
```

**现实：**
```
→ 卡在坐标映射的 Bug 里一个月出不来
→ 用户的手脑作为"连接器"就够了
→ 等有了 100 个付费用户再考虑
```

---

## 💼 商业策略（COO 视角）

### 1. 双线练手策略

**PlanSnap（广度）：**
```
目标用户：装修师傅、电工、房东
市场特点：用户量大
练手内容：
- Stripe 支付
- PWA 离线稳定性
- 大规模用户获取
```

**IsoSnap（深度）：**
```
目标用户：管工
市场特点：用户群小但粘性极高
练手内容：
- Node-based 核心逻辑
- 专业级功能（Export / Pro License）
- 付费意愿测试
```

---

### 2. 品牌错位策略

**独立域名（COO 建议）：**
```
PlanSnap: plan.snaplabs.io
IsoSnap:  iso.snaplabs.io

→ 虽然共用 Auth 账号
→ 但商业推广完全独立
```

**目标：**
- 避免用户认知混淆
- 分别建立品牌心智
- 独立市场推广

---

### 3. 定价策略

**许可模式：**
```
单品：$19 / 产品 / 月
全家桶：$49 / 月（PlanSnap + IsoSnap）

→ 第一天就要写得足够通用
```

**收入预期：**
- PlanSnap：规模化收入（用户多）
- IsoSnap：高ARPU收入（单价高）

---

## ✅ COO 的验收标准（练手闭环）

### 1. 匿名试用无感切换 🔴

**要求：**
```
用户在不登录情况下画完
→ 点击保存
→ 弹出注册框
→ 注册完后图纸必须毫秒级同步到新账号
→ 不能丢数据
```

**技术难点：**
- 匿名会话管理
- 数据迁移
- 状态同步

---

### 2. 支付网关极速响应 🔴

**要求：**
```
从点击"解锁导出"
→ 到 Stripe 支付成功
→ 再到下载 PDF

整个过程不能超过 60 秒
```

**用户体验：**
- 无缝支付
- 立即可用
- 零等待感

---

### 3. PDF 导出工程级质量 🔴

**PlanSnap 导出：**
```
要像建筑师画的
- 清晰的线条
- 专业的标注
- 规范的图框
```

**IsoSnap 导出：**
```
要像教科书上的
- 精确的等轴角度
- 清晰的管径标注
- 专业的 fitting 符号
```

**COO 观点：**
> **这种"高级感"决定了用户是否愿意付那 $49**

---

## 🎯 CDO 的技术架构建议

### 架构设计原则

**单一代码库，双产品构建：**

```
/monorepo
  /packages
    /core           # 共享核心（刀柄）
    /plan-snap      # PlanSnap 独立包（刀刃）
    /iso-snap       # IsoSnap 独立包（刀刃）
    /shared-ui      # 可选：极少量共享组件
```

---

### 数据模型设计

**共享基础模型：**

```typescript
// Core 包
interface Node {
  id: string;
  x: number;
  y: number;
  type: 'plan' | 'iso'; // 区分来源
}

interface Edge {
  id: string;
  fromNodeId: string;
  toNodeId: string;
  metadata: Record<string, any>; // 产品特定数据
}

// 基础 Canvas 引擎
class CanvasEngine {
  render(): void;
  export(): Blob;
}
```

---

**产品特定扩展：**

```typescript
// PlanSnap 包
interface PlanEdge extends Edge {
  metadata: {
    wallType: 'exterior' | 'interior';
    thickness: number;
  };
}

// IsoSnap 包
interface IsoEdge extends Edge {
  metadata: {
    pipeType: 'drain' | 'vent';
    diameter: string;
    direction: 'vertical' | 'left-down' | 'right-down';
  };
}
```

---

### 存储策略

**本地存储（MVP）：**

```typescript
// 共享存储引擎
class StorageEngine {
  // PlanSnap 数据
  async savePlan(projectId: string, data: PlanData): Promise<void>;
  
  // IsoSnap 数据
  async saveIso(projectId: string, data: IsoData): Promise<void>;
  
  // 共享：用户、授权
  async saveUser(user: User): Promise<void>;
  async saveLicense(license: License): Promise<void>;
}
```

**未来云同步：**
```
Phase 2: Supabase / Firebase
- 增量同步
- 冲突解决
- 离线优先
```

---

### 导出引擎

**共享导出管道：**

```typescript
class ExportEngine {
  // 共享的导出流程
  async export(format: 'pdf' | 'png'): Promise<Blob> {
    const canvas = this.render();
    const withHeader = this.addHeader(canvas);
    const withFooter = this.addFooter(withHeader);
    
    if (format === 'pdf') {
      return this.canvasToPDF(withFooter);
    } else {
      return this.canvasToPNG(withFooter);
    }
  }
  
  // 产品特定的渲染逻辑
  abstract render(): Canvas;
  abstract addHeader(canvas: Canvas): Canvas;
  abstract addFooter(canvas: Canvas): Canvas;
}

// PlanSnap 实现
class PlanExportEngine extends ExportEngine {
  render() {
    // 2D 平面渲染
  }
  
  addHeader(canvas) {
    // 建筑师风格图框
  }
}

// IsoSnap 实现
class IsoExportEngine extends ExportEngine {
  render() {
    // 等轴测渲染
  }
  
  addHeader(canvas) {
    // 工程图纸风格图框
  }
}
```

---

### 认证与支付

**共享的 Auth/Payment 服务：**

```typescript
// 认证服务
class AuthService {
  async login(email: string, password: string): Promise<User>;
  async register(email: string, password: string): Promise<User>;
  async migrateAnonymousData(userId: string): Promise<void>;
}

// 支付服务
class PaymentService {
  async createCheckout(
    userId: string,
    product: 'plan' | 'iso' | 'bundle'
  ): Promise<string>; // Stripe Checkout URL
  
  async verifyLicense(userId: string, product: string): Promise<boolean>;
}

// 许可管理
interface License {
  userId: string;
  products: ('plan' | 'iso')[];
  tier: 'free' | 'pro';
  expiresAt: Date;
}
```

---

## 🚨 关键风险与预警

### 风险 1：功能蔓延 🔴

**症状：**
```
"我觉得 PlanSnap 加一个等轴视图会很酷..."
"IsoSnap 能不能顺手加个平面图预览..."
```

**预防措施：**
```
1. CTO 必须在 PR 中注明功能属于哪个产品
2. 任何跨产品功能必须 CPO 批准
3. 每周 review 产品边界遵守情况
```

---

### 风险 2：UI 合并诱惑 🔴

**症状：**
```
"工具栏可以共用，用 tab 切换..."
"快捷键可以统一..."
```

**预防措施：**
```
1. 两个产品分别设计 UI
2. 共享组件仅限于：登录框、支付弹窗、导出对话框
3. 工具栏、画布、交互逻辑完全隔离
```

---

### 风险 3：过早优化 🟡

**症状：**
```
"我们先把 2D 转 ISO 的算法做出来..."
"先把 Code Check 框架搭好..."
```

**预防措施：**
```
1. Phase 2/3 功能明确列入"禁止清单"
2. 专注于 MVP 验收标准
3. 有了 100 个付费用户再考虑
```

---

## 📋 执行计划

### Sprint 1（Week 1-2）：架构搭建

**目标：** 建立共享基础设施（刀柄）

**任务：**
- [ ] 建立 monorepo 结构
- [ ] 实现 CanvasEngine 基类
- [ ] 实现 StorageEngine（IndexedDB）
- [ ] 实现 ExportEngine 基类
- [ ] 实现 AuthService（基础登录/注册）
- [ ] 集成 Stripe（基础支付流程）

**验收：**
- [ ] 两个产品可以独立构建
- [ ] 共享代码在 /core 包中
- [ ] 本地存储可用
- [ ] 可以导出 PNG

---

### Sprint 2（Week 3-4）：IsoSnap MVP

**目标：** 完成 IsoSnap 可用版本

**任务：**
- [ ] 节点系统（Task 1-5，参考之前的执行指南）
- [ ] Fixture 显示
- [ ] 管径标注
- [ ] 导出 PDF（工程图纸风格）
- [ ] PWA 基础配置

**验收：**
- [ ] 能画一套 WC + lav + vent 系统
- [ ] 节点可以连接
- [ ] 框选和拖动可用
- [ ] 导出的 PDF 像教科书

---

### Sprint 3（Week 5-6）：PlanSnap MVP

**目标：** 完成 PlanSnap 可用版本

**任务：**
- [ ] 2D 平面绘图引擎
- [ ] 房间/墙体/门窗工具
- [ ] 拖拽/拉伸/对齐
- [ ] 尺寸标注
- [ ] 导出 PDF（建筑师风格）

**验收：**
- [ ] 能画一套简单的平面图
- [ ] 拖拽交互流畅
- [ ] 导出的 PDF 像建筑师画的

---

### Sprint 4（Week 7-8）：支付与验收

**目标：** 完成"练手闭环"

**任务：**
- [ ] 匿名试用 → 注册无缝切换
- [ ] Stripe 支付集成测试
- [ ] 单品 vs 全家桶定价
- [ ] PDF 导出质量优化
- [ ] PWA 离线功能完善

**验收（COO 标准）：**
- [ ] 匿名试用无感切换
- [ ] 支付流程 < 60 秒
- [ ] PDF 导出工程级质量

---

## 🎯 成功标准

### MVP 成功 = 3 个指标

**1. 产品边界清晰：**
```
✅ CTO 能明确区分"这是 PlanSnap 还是 IsoSnap 的功能"
✅ 没有功能跨界
✅ 交互逻辑完全独立
```

**2. 练手闭环完成：**
```
✅ 注册到收费流程跑通
✅ 两个产品都有付费用户
✅ Stripe 支付稳定
```

**3. 产品可用性：**
```
✅ PlanSnap：普通人 10 分钟画完一套平面图
✅ IsoSnap：管工 10 分钟画完一套 isometric
✅ 导出 PDF 质量达到"愿意付 $49"的水平
```

---

## 💬 给 CTO 的底线（重申）

### CPO 的底线

> **如果一个功能让你犹豫"这是 PlanSnap 还是 IsoSnap 的功能"，**  
> **那这个功能现在不该存在。**

---

### COO 的底线

> **创业早期，最容易犯的错误就是"顺手加个功能"。**  
> **这份清单通过硬隔离（Hard Isolation），确保产品以最轻量、最锋利的姿态切入市场。**

---

### CDO 的底线

> **共用技术栈是为了效率，但不能以牺牲产品心智模型为代价。**  
> **两个产品 = 两套交互哲学 = 两个独立的代码包。**

---

## 📎 附录：决策清单

### 已确认的关键决策

| 决策 | 负责人 | 状态 |
|------|--------|------|
| 双产品线战略 | CEO + CPO + COO | ✅ 确认 |
| 产品边界不可越界 | CPO | ✅ 确认 |
| 硬隔离原则 | CPO + CTO | ✅ 确认 |
| 双域名策略 | COO | ✅ 确认 |
| 定价模型 | COO | ✅ 确认 |
| 验收标准 | COO | ✅ 确认 |
| 技术架构 | CDO + CTO | ⏳ 待实施 |

---

### 待讨论的问题

| 问题 | 负责人 | 优先级 |
|------|--------|--------|
| 具体域名注册 | COO | 🟡 P1 |
| Stripe 账号设置 | COO | 🟡 P1 |
| 产品 Logo 设计 | 设计团队 | 🟢 P2 |
| 市场推广策略 | COO | 🟢 P2 |

---

## 📊 里程碑时间表

| 里程碑 | 时间 | 关键输出 |
|--------|------|----------|
| M1: 架构搭建 | Week 1-2 | 共享基础设施 |
| M2: IsoSnap MVP | Week 3-4 | 可用的 IsoSnap |
| M3: PlanSnap MVP | Week 5-6 | 可用的 PlanSnap |
| M4: 练手闭环 | Week 7-8 | 支付+导出完成 |
| M5: Beta 测试 | Week 9-10 | 温哥华本地测试 |
| M6: 正式发布 | Week 11-12 | 公开发布 |

---

## 🎓 关键洞察（牢记）

### CPO 的产品哲学

> **两款"心智工具"，不是同一产品的两个模式。**

---

### COO 的运营智慧

> **这不是在教 CTO 怎么写代码，而是在保命。**

---

### CDO 的技术原则

> **刀柄共用，刀刃分离。**  
> **共享基础设施，独立产品逻辑。**

---

## ✅ 下一步行动

### 立即执行（本周）

1. **CTO：** 建立 monorepo 结构
2. **设计：** 分别设计 PlanSnap 和 IsoSnap 的 UI
3. **COO：** 注册独立域名
4. **CEO：** 确认预算和时间表

---

### 近期准备（2周内）

1. **CTO：** 完成共享基础设施
2. **团队：** Sprint 2 开始 IsoSnap 开发
3. **COO：** 准备 Stripe 账号和定价页面

---

**版本：** v1.0  
**创建时间：** 2026-02-02  
**下次审查：** Sprint 1 完成后  
**优先级：** 🔴 战略级

---

**这是一个清醒、克制、可执行的产品战略。双产品线不是为了"更大"，而是为了"更对"。** 🚀
